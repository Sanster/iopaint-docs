# Remove Background

[Remove background](https://github.com/danielgatis/rembg) from the picture or generate masks for foreground/main subject objects.
![rembg](../../public/rembg.png)

Install dependency package:

```bash
pip install rembg
```

Command line argument to enable Remove Background(always run on CPU):

```bash
--enable-remove-bg
```